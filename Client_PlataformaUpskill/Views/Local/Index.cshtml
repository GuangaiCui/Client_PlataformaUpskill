@model IEnumerable<PlataformaUpskill_API.Data.Models.Local>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-controller="Local" asp-action="LocalCreate">Create New Local</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>

                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Local" asp-action="SalaIndex" asp-route-id="@item.Id"
                       class="btn btn-primary mx-2">  Details</a>
                        <a asp-controller="Local" asp-action="LocalEdit" asp-route-id="@item.Id"
                       class="btn btn-primary mx-2">  Edit</a>

                        <form asp-action="LocalDelete" asp-route-id="@item.Id">
                            <input type="submit" value="Delete" class="btn btn-danger mx-2" />
                        </form>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
